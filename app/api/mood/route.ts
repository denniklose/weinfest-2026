import { NextResponse } from "next/server";
import { saveMoodVote } from "../../../lib/store";
export async function POST(request: Request) { try { const body = await request.json() as { token?: unknown; participantId?: unknown }; if (typeof body.token !== "string" || typeof body.participantId !== "string") return NextResponse.json({ error: "Bitte wähle zuerst eine Person." }, { status: 400 }); await saveMoodVote(body.token, body.participantId); return NextResponse.json({ ok: true }); } catch (error) { const code = error instanceof Error ? error.message : ""; if (code === "SESSION") return NextResponse.json({ error: "Bitte melde dich zuerst an." }, { status: 401 }); if (code === "PERSON") return NextResponse.json({ error: "Diese Person ist nicht verfügbar." }, { status: 404 }); return NextResponse.json({ error: "Die Stimmung konnte nicht gespeichert werden." }, { status: 500 }); } }
